package flashbot.core

import flashbot.models.api.OrderTarget

//class OrderRequest(targetSpec: OrderTarget)(implicit ctx: TradingSession) {
//
//
//  def send(): OrderTarget = {
//    val target = transformTarget(targetSpec)
//    ctx.send(target)
//    target
//  }
//}
